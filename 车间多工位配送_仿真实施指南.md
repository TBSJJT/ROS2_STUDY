# 车间多工位配送机器人：仿真实施指南

课题：**《面向车间多工位配送的移动机器人多目标路径优化研究》**  
日期：2026-09-19  
工程：<https://github.com/TBSJJT/ROS2_STUDY>  
代码评估基线：`d729712`。本文依据此前对该版本的源码检查制定，未实际运行 Ubuntu、ROS 或 Gazebo。下文“新增”文件、接口和功能均为实施计划，不代表仓库已经实现。

## 工程实施状态（2026-09-21）

已落地六设备、六工位、物料站场景及实际 SLAM 地图。定位配送入口为
`ros2 launch gasrobot_bringup delivery_sim.launch.py`，不同时启动 SLAM。
`gasrobot_delivery` 保存独立工位、批次、仿真导航参数，以及配送状态机和运行日志。
启动系统不会自动配送，使用 `/delivery_manager/start` 显式确认装货。

六工位往返与跨排导航、真实暂停/取消操作、短路线及连续三批完整配送已通过验收；
实际记录与精度限制以 [验收记录](src/gasrobot_delivery/docs/acceptance.md) 为准，
完整命令见 [配送操作说明](src/gasrobot_delivery/README.md)。下文仍保留后续研究规划，
代价矩阵和 NSGA-II 尚未实施。

## 1. 先固定研究边界

- 一个物料暂存站、多个固定生产工位。
- 一台机器人服务一个限定车间配送单元。
- 一批任务在出发前确定，全部物料一次装齐，总载荷不超过机器人承载能力。
- 每个任务工位访问并交付一次，最后返回物料站。
- 人工装卸；仿真用服务时间模拟卸货，不做机械臂和图像处理。
- 研究两个目标：总配送距离、累计迟交时间。
- NSGA-II 优化工位访问顺序，Nav2 负责点间导航。
- 第一版不考虑多机器人、动态插单、途中补货、充电调度和自动装卸。

最终闭环：车间环境 → 建图定位 → 单点导航 → 固定顺序配送 → 距离/时间矩阵 → 顺序优化 → 仿真执行 → 数据分析。

## 2. 软件环境与实施原则

现有仓库采用 ROS 2 Humble、Gazebo Classic 风格的 `gazebo_ros` 和 `gazebo_ros2_control`。优先在已有、能正常运行的 Ubuntu 22.04/Humble 仿真环境中继续工作，不在同一次改造中更换机器人模型、仿真平台和导航框架。

注意：Gazebo Classic 已于 2025 年 1 月结束支持。继续使用它是复用现有工程的短期选择，不代表它是当前推荐的新项目平台。保留虚拟机快照、依赖版本和安装说明；若旧环境无法可靠恢复，应先单独确定迁移方案，不能直接混用新版 Gazebo 的插件和 Classic 插件。[官方说明](https://classic.gazebosim.org/)

本文所有 Bash 命令均在 Ubuntu ROS 工作空间执行，不是在 Windows PowerShell 执行。工作空间路径请替换成自己的实际路径。命令中出现的新文件须先创建、安装并重新编译。

开始前记录：

```bash
lsb_release -a
printenv ROS_DISTRO
gazebo --version
git rev-parse HEAD
git status --short
```

先保留原工程，不删除气体相关包，不批量重命名 `gasrobot_`。新配送入口不启动气体和旧巡检任务即可。

## 3. 第一阶段：先跑通原始 Gazebo 仿真

在实际 ROS 工作空间根目录执行：

```bash
source /opt/ros/humble/setup.bash
colcon build --packages-up-to gasrobot_simulation --symlink-install
source install/setup.bash
ros2 launch gasrobot_simulation gazebo_sim.launch.py
```

这一步只验证原环境，不接 NSGA-II，不改导航参数。

另开已加载 ROS 环境的终端检查：

```bash
ros2 control list_controllers
ros2 topic list -t
ros2 topic info /clock -v
ros2 run tf2_ros tf2_echo odom base_footprint
```

根据实际话题列表找到激光、里程计和速度输入，检查消息类型、发布者和订阅者。不要凭话题名猜测已经接通。

验收：

- [ ] Gazebo 世界与机器人正常加载，无持续插件错误。
- [ ] `diff_drive_controller` 和关节状态广播器正常激活。
- [ ] 机器人可低速前进、转弯、停止。
- [ ] 雷达看到墙体和障碍物。
- [ ] 里程计与 TF 连续，仿真时间正常。
- [ ] 关闭控制命令后机器人能停止，不存在两个控制器抢占轮关节。

交付物：环境版本记录、原场景截图、启动日志、基础问题清单。

## 4. 第二阶段：搭建车间 Gazebo 场景

### 4.1 第一张地图的建议布局

先搭一个约 12 m × 10 m 的矩形限定作业区。使用简单长方体模拟设备，保留环绕通道与设备间连接通道；不用复杂工厂模型和纹理。

以下为 **Gazebo world 坐标下的初始设计值**，不是已验证地图坐标。机器人出生点、墙体厚度、车体尺寸和障碍膨胀会影响实际可通行性，必须在 RViz 和导航测试中验证。

| 对象 | 中心位置或范围（m） | 尺寸/说明 |
|---|---|---|
| 作业区 | x：0～12，y：0～10 | 四周边界墙，厚约 0.15 m |
| 设备 A、B、C（第一排） | (3.5, 3)、(6.5, 3)、(9.5, 3) | 每台 2 × 1.6 × 1 m |
| 设备 D、E、F（第二排） | (3.5, 7)、(6.5, 7)、(9.5, 7) | 每台 2 × 1.6 × 1 m |
| 物料站料架 | (1.5, 0.45) | 绿色三层料架、橙色料箱，带碰撞体 |
| 物料站候选停靠点 | (1.5, 1.5) | 绿色无碰撞地面标记，与料架分离 |
| S1、S2、S3 候选停靠点 | (3.5, 1.5)、(6.5, 1.5)、(9.5, 1.5) | 对应 A、B、C，朝向 +Y |
| S4、S5、S6 候选停靠点 | (3.5, 8.5)、(6.5, 8.5)、(9.5, 8.5) | 对应 D、E、F，朝向 −Y |

设备高度要覆盖激光扫描平面，避免视觉上有障碍、激光却扫描不到。工位是可停靠的位置，不是让机器人驶入工作台内部。

固定每个工位的交付朝向，并在全部方案中保持一致；不要按某条优化路线临时改变工位朝向，否则点间时间矩阵不再一致。

### 4.2 需要新增/修改的文件

- 新增 `src/gasrobot_simulation/worlds/workshop_delivery_v1.world`。
- 可选新增 `src/gasrobot_simulation/models/workshop_equipment/`，供重复设备复用。
- 修改 `src/gasrobot_simulation/launch/gazebo_sim.launch.py`：将当前写死的出生位姿改为 `spawn_x/y/z/yaw` 参数，保留原默认行为。
- 检查 `gasrobot_simulation/CMakeLists.txt` 安装规则，确保新 world/model 会被安装。

建议直接用 SDF 编辑可追踪的尺寸和坐标；GUI 适合预览，但最终必须保存到仓库。每个设备模型设置 `static=true`，同时提供 `visual` 与 `collision`。装卸点标记仅作为视觉标识，不要误加阻挡机器人通行的碰撞体。

新增场景并编译安装后，可以复用已有 `world` 参数：

```bash
ros2 launch gasrobot_simulation gazebo_sim.launch.py \
  world:="$(ros2 pkg prefix gasrobot_simulation)/share/gasrobot_simulation/worlds/workshop_delivery_v1.world"
```

出生参数的命令行名称以实施后的 launch 为准。原 launch 将出生点写死在 (0,0) 附近，新场景有边界墙，必须先改出生点再使用。

### 4.3 这一阶段不做什么

不增加行人、不做动态障碍、不研究货物跌落，不为了漂亮模型引入大体积资源。先证明基本配送布局可通行。

验收：机器人能从物料站手动走到 6 个候选工位，通道中可转弯，设备没有穿模，所有工位有足够停车空间。

## 5. 第三阶段：接通仿真导航并建图

### 5.1 先解决仿真与实车的话题差异

基础 Gazebo 入口仅启动仿真、机器人与控制器；现已增加独立的定位配送组合入口。实车整机入口包含硬件节点，不能仅设置 `use_sim_time=true` 就直接当作仿真入口。

`src/gasrobot_bringup/launch/delivery_sim.launch.py` 组合仿真、AMCL 定位、Nav2 与配送任务；建图使用独立 SLAM 入口，避免同时发布 map → odom。

| 接口 | 需要验证的内容 |
|---|---|
| 速度 | Nav2 最终速度输出接到差速控制器真实订阅的话题，类型一致 |
| 里程计 | 控制器输出映射到导航使用的话题，通常为 `/odom` |
| 激光 | 实际扫描话题与 SLAM、AMCL、代价地图配置一致 |
| TF | `map → odom → base_footprint → 激光坐标系` 完整 |
| 仿真时间 | SLAM/AMCL、Nav2、任务节点、记录节点均使用 `/clock` |

现有仿真控制器设置 `use_stamped_vel=false`。常见输入是 `/diff_drive_controller/cmd_vel_unstamped`，但必须通过实际话题查询确认后再 remap；不能同时启动两个节点向底盘下发任务速度。

`odom → base_footprint` 应只有一个来源；仿真不要再启动 STM32 里程计发布。建图时由 SLAM 发布 `map → odom`，定位时由 AMCL 发布，不同时运行两者争用同一变换。

### 5.2 建图和保存

复用 `gasrobot_gas_mapping/launch/mapping.launch.py`，开启仿真时间。低速遥控走遍通道，形成完整地图，再保存：

```bash
ros2 run nav2_map_server map_saver_cli -f workshop_delivery_v1 \
  --ros-args -p use_sim_time:=true
```

命令将地图保存到当前目录。将生成的 PGM/YAML 一起纳入地图目录，并确认 YAML 内的图像路径有效。加载保存地图后关闭建图节点，运行 AMCL 和 Nav2。

重要：Gazebo world 坐标与 SLAM 的 map 坐标不保证一致。必须在 RViz 的 `map` 坐标系重新确认物料站、工位位姿，不能直接复制第 4 节坐标作为导航目标。

### 5.3 单点导航验证

用 RViz 初始化机器人真实对应位姿，然后逐个发送工位目标。保持现有低速策略，先不优化 Nav2 算法。

验收：

- [ ] 所有工位均位于已知自由区域，并满足车体及安全距离要求。
- [ ] 物料站到各工位、各工位返回物料站均可到达。
- [ ] 跨设备的路线绕开障碍，不穿设备。
- [ ] 定位稳定，终点位置/朝向满足预先设定的交付容差。
- [ ] 配送托盘若改变外形，已同步调整 footprint/碰撞模型。

交付物：世界文件、地图、工位坐标表、导航配置、单点导航测试记录。

## 6. 第四阶段：先完成固定顺序配送

新增 `gasrobot_delivery` 包。第一版只做“物料站 → S1 → S3 → S5 → 物料站”，不接优化器。

建议文件：

```text
gasrobot_delivery/
├── config/stations.yaml
├── config/batch.yaml
├── gasrobot_delivery/delivery_manager.py
├── gasrobot_delivery/experiment_logger.py
├── launch/delivery.launch.py
└── test/
```

配置分开保存：`stations.yaml` 保存固定地图位姿；`batch.yaml` 保存任务工位、截止时间和服务时间。首版一批中每工位最多一个任务。

状态机：

```text
IDLE → 等待装货确认 → NAVIGATING → SERVICING
                         ↑             ↓
                         └── 下一个工位 ┘
全部交付 → RETURNING → COMPLETED
异常分支：FAILED / CANCELLED / PAUSED
```

一次只发送一个 `NavigateToPose` 目标。到达工位后停留预设服务时间；仿真自动确认，未来实机改成人工确认。服务结束才记录该工位交付完成。

现有巡检管理器的超时、取消、重试实现可以借鉴，但不要保留气体事件依赖，也不要把“到达航点”直接当成“交付完成”。旧巡检管理器在配送模式中必须禁用。

业务暂停和卸货等待均计入实际配送耗时。Gazebo 全局暂停不推进仿真时钟，不应混同业务暂停。任务超时与服务计时统一使用 ROS 仿真时间，算法运算耗时另外用墙钟记录。

验收：固定顺序能完整执行；取消后停止继续发目标；失败工位不会被记成成功；回到物料站才结束本批。

## 7. 第五阶段：建立距离与迟交模型

### 7.1 点间代价矩阵

新增 `cost_matrix_builder.py`。对于物料站和 n 个工位，计算有向的 (n+1) × (n+1) 距离矩阵 L 和时间矩阵 T。

- 使用固定地图、固定机器人 footprint、固定规划参数。
- 通过 Nav2 规划服务/Action 能力获取点间路径，例如 `ComputePathToPose`；按当前 Humble 接口定义实现，不照搬其他版本字段。
- 显式提供每对起终点的 map 位姿，按路径连续点的距离求和。
- 不可达的点对标记为不可达并在规划前处理，不能用直线距离填补。
- 固定缓存版本：地图哈希、工位位姿、规划器参数、机器人配置。
- 不强制令 Lij=Lji 或 Tij=Tji，转向和规划实现可能造成差异。
- 算法迭代读取缓存，不能每评价一个排列都调用 Gazebo。

时间初版可用 Lij/v 加统一的可解释时间项；后续用独立导航标定数据估计 Tij。不要直接把被比较方案的实测结果回填为该方案的预测值。

### 7.2 目标和时间口径

用 0 表示物料站，π 表示工位排列，并令 π0=π(n+1)=0：

```text
f1 = Σ(k=0..n) L[πk, π(k+1)]
C0 = 0
C[πk] = C[π(k-1)] + T[π(k-1), πk] + service[πk]
f2 = Σ(k=1..n) max(0, C[πk] - due[πk])
```

期限是从本批出发时刻起算的软截止时间。迟交允许发生，但进入惩罚目标；不是把所有迟交解判为不可行。回程计入距离和总任务时长，不计入已完成工位的迟交。

单元测试至少覆盖：遗漏/重复工位、空批次、不可达、刚好按时、服务时间引起迟交、回程距离、时间单位一致。用 5～8 个点枚举排列检查目标计算和小规模 Pareto 解集。

## 8. 第六阶段：接入标准与改进 NSGA-II

新增 `delivery_planner` 包，核心算法不要依赖 ROS 节点，使其能独立批量运行。

按顺序实现：

1. 最近邻顺序；最早截止期优先顺序。
2. 标准 NSGA-II：排列编码、合法的顺序交叉、交换或逆序变异。
3. 改进 A：距离启发式、期限启发式、随机排列混合初始化。
4. 改进 B：迟交引导的插入邻域搜索，并重算全部受影响任务。
5. 对照与消融：标准版、仅 A、仅 B、A+B；加入随机插入控制，辨别收益是否只是额外计算造成。

这两个改进是待验证方案，不能预先写成“已证明更优”或仅凭名称宣称创新。局部搜索评价次数计入总预算，各算法采用公平预算；记录随机种子和实际耗时。

输出保存整个 Pareto 解集和每条路线的目标值。机器人只执行其中一条，选择规则必须在测试前固定，例如：在相对固定参考路线允许的距离增加范围内，选择累计迟交最小者。距离阈值、平局规则和无候选时的处理均写入配置。

## 9. 第七阶段：设计两层仿真实验

### 9.1 离线优化实验：证明算法效果

- 小规模 5～8 个任务：与枚举结果核对。
- 中大规模可选 10、20、40 个任务：采用相应规模、容量合理的合成配送单元，不在原 6 工位场景中虚构不存在的工位。
- 布局至少两类：规则双排布局、带通道绕行的布局。
- 截止时间设置宽松、适中、紧张三个等级。
- 用固定、公开的参考顺序和规则生成期限，再打乱部分任务紧迫程度；各算法共用完全相同的实例，不能按改进算法输出倒推期限。
- 每种随机算法建议独立运行至少 20 次，固定并公开种子列表。
- 参数调整只使用训练/调试实例，正式测试实例单独保留。

指标：路线距离、累计迟交、准时交付率、运算耗时、共同归一化与共同参考点下的 Hypervolume。单路线启发式与 Pareto 算法比较时，说明比较的是单点还是解集；不能仅看一条最好结果。

### 9.2 Gazebo 执行实验：证明方案可执行

先用 6 个工位；选少量有代表性的批次执行最近邻、最早截止期、标准 NSGA-II 和改进方案。初步每方案重复 5 次，必要时根据波动追加。

比较时保持地图、初始位姿、速度限制、Nav2 参数、服务时间和传感器设置一致。每次复位、确认定位后再出发，不能让某算法多承担上一轮残留状态。

记录预测与实际的差异，而不是只画路径：

- 预测距离与实际行驶距离。
- 预测交付完成时间与实际完成时间。
- 预测累计迟交与实际累计迟交。
- 任务成功率、导航重试、人工干预次数。

可选增加服务时间扰动实验，但应在基本实验完成后再做。先不加动态行人和在线重规划。

## 10. 日志和论文图表

每次运行独立目录，保存：

```text
run_id/
├── metadata.json          # 版本、地图、参数、种子、算法、方案选择规则
├── batch.yaml             # 本次任务快照
├── plan.json              # 顺序、预计距离、预计交付时刻、Pareto 解集
├── events.csv             # 出发、到达、交付完成、返回、失败/取消
├── trajectory.csv         # 时间戳与机器人轨迹
└── summary.json           # 距离、迟交、成功率、时间预测误差
```

`events.csv` 至少包含 batch_id、station_id、event_type、ros_time、elapsed_time、due_sec、status。

实际距离采用一致的轨迹积分口径；若使用 Gazebo 真值轨迹，仅用于评估，不反馈给导航和优化器。若使用里程计，要注明漂移影响；不要把地图定位跳变算成真实行驶距离。

失败/超时运行必须保留，不能删除后只统计成功试验。可分别报告成功率及成功运行条件下的距离/迟交，但要清楚标注统计口径。

论文最少形成：场景与地图图、系统架构图、配送状态机图、模型说明、Pareto 前沿、算法对比/消融表、执行轨迹、预测与实测误差图。

## 11. 建议实施顺序与阶段交付

以下是工作安排参考，不是完成时间保证；每阶段通过验收再推进。

| 阶段 | 主要工作 | 交付物 |
|---|---|---|
| A | 原仿真运行、话题和 TF 排查 | 基线环境记录 |
| B | 车间 world、设备与停靠点 | 可驾驶的车间场景 |
| C | SLAM、AMCL、Nav2 集成 | 地图与单点导航测试 |
| D | 固定顺序配送、服务时间、日志 | 3 工位配送闭环 |
| E | 距离/时间矩阵、模型与单元测试 | 可复现的任务实例 |
| F | 标准算法、改进算法、离线实验 | 对比与消融数据 |
| G | 优化顺序执行、重复验证 | Gazebo 实验与图表 |

当前优先级：**只先完成 A、B、C。先让机器人在车间环境中可靠到达各工位，不要一开始把 NSGA-II、任务管理和仿真同时调试。**

## 12. 交给后续开发者的第一阶段任务

> 请基于 TBSJJT/ROS2_STUDY 的实际工作副本，从 Gazebo 车间环境开始完成仿真基础。先检查 AGENTS.md、工作区未提交变更及 ROS/Gazebo 版本，保留现有代码。新增 workshop_delivery_v1.world，采用一个物料站、两排设备和六个工位候选点；将机器人出生位姿参数化，保证出生点无碰撞。复用现有底盘和传感器模型，核对速度、里程计、激光、TF 和仿真时钟接口。不得启动真实 STM32、真实雷达、气体检测或旧巡检任务。先完成场景加载、低速运动、激光和 TF 验证，再增加独立的仿真建图/导航组合入口。不要在这一阶段实现 NSGA-II，不批量重命名旧包，不覆盖已有地图和路线。列出实际修改文件、运行命令、验收证据和未完成项；没有运行条件的检查必须标明未验证。

## 13. 依据与参考入口

- [仓库基线](https://github.com/TBSJJT/ROS2_STUDY/tree/d72971215cbe525d92c01e9878a0cf12b8775815)：工程结构与当前实现依据。
- [Gazebo 仿真入口](https://github.com/TBSJJT/ROS2_STUDY/blob/d72971215cbe525d92c01e9878a0cf12b8775815/src/gasrobot_simulation/launch/gazebo_sim.launch.py)：world 参数、出生位姿和控制器启动。
- [控制器参数](https://github.com/TBSJJT/ROS2_STUDY/blob/d72971215cbe525d92c01e9878a0cf12b8775815/src/gasrobot_simulation/config/gasrobot_ros2_controller.yaml)：仿真差速控制与速度接口。
- [导航入口](https://github.com/TBSJJT/ROS2_STUDY/blob/d72971215cbe525d92c01e9878a0cf12b8775815/src/gasrobot_navigation/launch/navigation.launch.py)：复用 Nav2 Bringup。
- [Gazebo Classic 官方说明](https://classic.gazebosim.org/)：生命周期和迁移说明。

本指南是工程与实验实施方案，不是已完成的实验报告；不预设性能提升比例，也不代替导师对论文创新性与工作量的最终确认。
